class Env {
  /// Base URL of your Node.js backend (without trailing slash).
  /// For Windows development, use localhost
  static const String apiBase = 'http://localhost:5000/api';
  // If you run Android emulator, use: 'http://10.0.2.2:3000/api'
}
